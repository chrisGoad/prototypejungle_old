#Modules needed for node:



these need to be done under each instance of node

cd /mnt/ebs0/prototypejungledev/node
and
cd /mnt/ebs0/prototypejungle/node

#aws
sudo npm install aws-sdk

#oauth:
sudo npm install oauth

#levelup (db for node: https://github.com/rvagg/node-levelup)
sudo npm install level
sudo npm update level

#https://npmjs.org/package/send#readme  for serving static files
#sudo npm install send NOT ANY MORE


#javascript compression
sudo npm install minify

#static file server
sudo npm install node-static

#see https://npmjs.org/package/follow-redirects
sudo npm install follow-redirects

#from https://www.npmjs.com/package/MD5
sudo npm install MD5

BELOW NOT NEEDED FOR CURRENT OPS
#javascript parsing for docs
 sudo npm install esprima

#html parsing
sudo npm install htmlparser2
