NOT IN USE ervice firebase.storage {
  match /b/project-5150272850535855811.appspot.com/o {
    match /{allPaths=**} {
      allow write: if request.auth != null;
      allow read;
    }
  }
}

service firebase.storage {
  match /b/project-5150272850535855811.appspot.com/o {
    match /{allPaths=**} {
      allow write: if allPaths[0] == request.auth.uid;
      allow read;
    }
  }
}


service firebase.storage {
  match /b/project-5150272850535855811.appspot.com/o {
    match /{allPaths=**} {
      allow write: if resource.name.matches(request.uid + '.*');
      allow read;
    }
  }
}



service firebase.storage {
  match /b/project-5150272850535855811.appspot.com/o {
    match /{allPaths=**} {
      allow read;
      match /{filename} {
        allow write: if filename.matches(request.uid + '.*');
     }
  }
}

service firebase.storage {
  match /b/project-5150272850535855811.appspot.com/o {
    match /{userId}/{path=*} {
      allow write: if request.auth.uid == userId;
    },
    match /{allPaths=**} {
      allow read;
    }
  }
}
