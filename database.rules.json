{
  "rules": {
    ".read": true,
    "directory":{
       "$uid":{
           ".write": "auth.uid === $uid"
        },
        "directory":{
            ".validate": "newData.isString() && newData.val().length < 200"
        }
    },
    "s":{
       "$uid":{
           ".write": "auth.uid == $uid"
        }
    }
  }
}
